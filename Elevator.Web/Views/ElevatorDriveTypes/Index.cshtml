@model IEnumerable<Elevator.Web.Models.EE.Dictionary.ElevatorDriveType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ElevatorDriveTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ElevatorDriveTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ElevatorDriveTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ElevatorDriveTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ElevatorDriveTypeId })
        </td>
    </tr>
}

</table>
