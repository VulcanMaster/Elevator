@model IEnumerable<Elevator.Web.Models.EE.Operational.ServiceBoard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Elevator.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IncidentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Elevator.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IncidentDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServiceBoardId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServiceBoardId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServiceBoardId })
        </td>
    </tr>
}

</table>
