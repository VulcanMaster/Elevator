@model IEnumerable<Elevator.Web.Models.EE.Dictionary.ClientsBank>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientsBankName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientsBankName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientsBankId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientsBankId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientsBankId })
        </td>
    </tr>
}

</table>
