@model IEnumerable<Elevator.Web.Models.EE.Dictionary.ElevatorView>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ElevatorViewName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ElevatorViewName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ElevatorViewId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ElevatorViewId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ElevatorViewId })
        </td>
    </tr>
}

</table>
