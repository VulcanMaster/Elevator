@model IEnumerable<Elevator.Web.Models.EE.Dictionary.ElevatorSuspensionType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ElevatorSuspensionTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ElevatorSuspensionTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ElevatorSuspensionTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ElevatorSuspensionTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ElevatorSuspensionTypeId })
        </td>
    </tr>
}

</table>
